# Dev requests go to the Vite origin and are proxied to the local server (see
# vite.config.ts). Same-origin is what lets the SameSite=Strict refresh cookie
# ride along; "/" makes getApiBase() return "" so every request is same-origin.
VITE_API_BASE=/
