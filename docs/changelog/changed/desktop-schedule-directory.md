- Desktop scheduled tasks now run in a working directory (your home directory by
  default) instead of requiring a project, keep their own run history with a live
  cron preview when creating one, and open each run's transcript from the
  Schedules view — fires no longer appear as sessions in the project sidebar.
